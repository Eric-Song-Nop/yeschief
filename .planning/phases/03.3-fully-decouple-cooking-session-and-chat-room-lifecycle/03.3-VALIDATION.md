---
phase: 03.3
slug: fully-decouple-cooking-session-and-chat-room-lifecycle
status: passed
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-03
verified: 2026-04-03T01:26:52Z
---

# Phase 03.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | API: `bun:test`; Agent: `vitest`; WebUI: `@playwright/test` |
| **Config file** | WebUI: `apps/webui/playwright.config.ts`; API: `none`; Agent: `none` |
| **Quick run command** | `bun test --cwd "apps/api" test/sessions-connect.test.ts test/sessions-room-cleanup.test.ts test/session-commands.test.ts` |
| **Full suite command** | `bun run --cwd "apps/api" test && bun run --cwd "apps/agent" test && bun run --cwd "apps/webui" test:e2e:realtime` |
| **Estimated runtime** | ~90 seconds |

---

## Sampling Rate

- **After every task commit:** Run `bun test --cwd "apps/api" test/sessions-connect.test.ts test/sessions-room-cleanup.test.ts`
- **After every plan wave:** Run `bun run --cwd "apps/agent" test`
- **Before `$gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 120 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 03.3-01-01 | 01 | 1 | SESS-03 | API integration | `bun test --cwd "apps/api" test/sessions-connect.test.ts test/sessions-room-cleanup.test.ts` | ✅ | ✅ green |
| 03.3-02-01 | 02 | 2 | SYST-02 | API integration | `bun test --cwd "apps/api" test/sessions-connect.test.ts test/sessions-room-cleanup.test.ts test/session-commands.test.ts` | ✅ | ✅ green |
| 03.3-03-01 | 03 | 3 | SYST-03 | agent smoke | `bun run --cwd "apps/agent" test` | ✅ | ✅ green |
| 03.3-03-02 | 03 | 3 | SESS-03 | browser e2e | `bun run --cwd "apps/webui" test:e2e:realtime` | ✅ | ✅ green |
| 03.3-04-01 | 04 | 4 | SESS-03 / SYST-02 / SYST-03 | full regression | `bun run --cwd "apps/api" test && bun run --cwd "apps/agent" test && bun run --cwd "apps/webui" test:e2e:realtime` | ✅ | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [x] `apps/api/test/sessions-connect.test.ts` — add legacy adopt and room replacement coverage
- [x] `apps/api/test/sessions-room-cleanup.test.ts` — assert cleanup reads persisted binding, not raw `sessionId`
- [x] `apps/agent/src/runtime-smoke.test.ts` — add metadata-only `sessionId` parsing and missing-metadata failure coverage
- [x] `apps/webui/test/recovery-idempotency.e2e.ts` — assert room identifier may change while `sessionId` remains stable

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| LiveKit runtime can adopt a legacy room where `roomName === sessionId` and keep the same business session | SESS-03 | Mocked tests cannot prove real remote room state across migration | Follow `03.3-HUMAN-UAT.md` Test 1: pre-create a legacy room, call `POST /sessions/:sessionId/connect`, confirm the same business `sessionId` remains and no second session appears |
| Room replacement / reconnect does not leave duplicate tutor audio in the real room | SYST-03 | Real linked participant and remote audio behavior is difficult to inject deterministically in automation | Follow `03.3-HUMAN-UAT.md` Test 2: force room replacement, reconnect in browser, confirm only one tutor audio stream remains and the UI still points to the same session |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 120s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** passed
