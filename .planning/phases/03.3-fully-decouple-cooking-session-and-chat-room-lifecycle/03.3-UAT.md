---
status: complete
phase: 03.3-fully-decouple-cooking-session-and-chat-room-lifecycle
source:
  - 03.3-01-SUMMARY.md
  - 03.3-02-SUMMARY.md
  - 03.3-03-SUMMARY.md
started: 2026-04-02T17:06:53Z
updated: 2026-04-02T17:23:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: 停掉当前运行中的 API、agent 和 WebUI 进程后，从干净状态重新启动它们，再打开 companion 创建一个新的 cooking session，并尝试进入 voice connect 流程。预期服务能正常冷启动，不出现 binding、migration、dispatch metadata 相关错误；session 可以正常创建/加载，语音连接入口可继续执行。
result: pass

### 2. Legacy room adopt keeps the same business session
expected: 为一个新建 session 预先准备 `roomName === sessionId` 的 legacy LiveKit room 后，连续两次调用 `POST /sessions/:sessionId/connect` 并刷新 WebUI，返回的 `sessionId` 始终不变，`binding.roomName` 仍指向 legacy room，恢复列表不会出现第二个 session。
result: pass

### 3. Room replacement reconnect keeps one session and one tutor stream
expected: 连接语音后删除当前 room binding，再通过 Reconnect 或刷新页面恢复同一个 cooking session。新的 `binding.roomName` 可以变化，但浏览器仍指向原始 `sessionId`，恢复列表不分叉，且最终只会听到一条 tutor 音频流。
result: pass

## Summary

total: 3
passed: 3
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps
